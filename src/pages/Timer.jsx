const Timer = () => {
  return (
    <form className="flex flex-col">
      <input
        type="text"
        className="border-gray-400 focus:border-blue-400 focus:ring-transparent"
      />
    </form>
  );
};

export default Timer;
